import { styled } from '@mui/material/styles';

const StyledForm = styled('form')`
	display: flex;
	flex-direction: column;
	max-width: 500px;
	gap: 20px;
	margin: 0 auto;
	padding-top: 20px;
`;

export { StyledForm };
