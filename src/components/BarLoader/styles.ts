import LinearProgress from '@mui/joy/LinearProgress';
import { styled } from '@mui/material/styles';

const StyledBarLoader = styled(LinearProgress)`
	max-width: 200px;
	margin: 10% auto;
`;

export { StyledBarLoader };
